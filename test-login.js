const https = require('https'); function req(body) { const data = JSON.stringify(body); const options = { hostname: 'recipe-hub-gamma-nine.vercel.app', port: 443, path: '/api/auth/sign-in/email', method: 'POST', headers: { 'Content-Type': 'application/json', 'Content-Length': data.length, 'Origin': 'https://recipe-hub-gamma-nine.vercel.app' } }; const req = https.request(options, res => { let d = ''; res.on('data', chunk => { d += chunk; }); res.on('end', () => { console.log(res.statusCode, d); }); }); req.write(data); req.end(); } req({email: 'test9@example.com', password: 'Password123'});  
