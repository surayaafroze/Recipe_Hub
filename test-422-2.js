const https = require('https'); function req(body) { const data = JSON.stringify(body); const options = { hostname: 'recipe-hub-gamma-nine.vercel.app', port: 443, path: '/api/auth/sign-up/email', method: 'POST', headers: { 'Content-Type': 'application/json', 'Content-Length': data.length, 'Origin': 'https://recipe-hub-gamma-nine.vercel.app' } }; const req = https.request(options, res => { let d = ''; res.on('data', chunk => { d += chunk; }); res.on('end', () => { console.log(res.statusCode, d); }); }); req.write(data); req.end(); } req({email: 'test12@example.com', password: 'pass', name: 'Test User 12'}); req({email: 'test13@example.com', password: 'Password123', name: 'Te'}); req({email: 'inv', password: 'Password123', name: 'Test User 14'});  
